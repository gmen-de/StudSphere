# Deny all direct web access to this directory (downloaded CSV files, import log).
# Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
# Apache 2.2 and earlier
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
